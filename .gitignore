# Build outputs
/build/
/build_x64/

# Release binaries (uploaded to GitHub Releases, not tracked in source)
/release/

# Vendored MinGW toolchain (fetched separately, not source)
/toolchains/

# Secrets
cortex_host.token

# Editor / OS cruft
.vs/
.vscode/
*.user
Thumbs.db
